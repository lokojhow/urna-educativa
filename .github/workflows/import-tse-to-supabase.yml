name: Import TSE para Supabase
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/import-tse-to-supabase.yml'
jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - name: Importar candidatos filtrados
        run: |
          set -e
          curl --show-error --silent 'https://dxvqiiawthxwkypvsxci.supabase.co/functions/v1/urna-sync-candidatos-tse'
          echo
          # nova execução após ajuste do índice do Senado
